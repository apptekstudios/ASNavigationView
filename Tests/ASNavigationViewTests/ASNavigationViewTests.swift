import XCTest
@testable import ASNavigationView

final class ASNavigationViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ASNavigationView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
